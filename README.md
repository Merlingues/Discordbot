# Discordbot
my own discord bot

installation de FFmpeg, python : sudo apt update && sudo apt install -y ffmpeg python3

activation de l'environnement virtuelle : source .venv/bin/activate

installation des dépendances python : pip install -r requirements.txt

lancement du bot : python main.py